在本科毕设中，所谓的“创新点”并不一定非要发明一个新算法（那属于博士研究），而是**针对现有方案的某个痛点，提出一种改进的思路、架构或交互模式**。

对于 Chat2Viz 方向，如果想让你的毕设创新点显得“明显且突出”，建议从以下 **4个具体的“研究型”切入点** 中任选其一：

### 创新点一：基于“视觉动作”的指代消解（跨模态对齐）
*   **传统做法：** 用户只能通过文字改图（如：“把颜色变红”）。
*   **你的创新：** **“点击/框选 + 文本”的联合推理。**
*   **具体逻辑：** 当用户在生成的图表上用鼠标勾选了一块区域（例如几个离群点），然后输入“解释这些数据”，系统需要计算这块区域对应的底层数据，并将其作为上下文发送给 LLM。
*   **研究高度：** 这涉及到了**视觉语义与自然语言的对齐**。你可以提出一种“视觉特征编码器”，将用户的交互操作转化为 LLM 可理解的语义标签。
*   **亮点：** 答辩时演示“指哪打哪”，这种跨模态交互非常具有视觉冲击力。

### 创新点二：引入统计约束的“逻辑守卫”机制（解决幻觉）
*   **传统做法：** LLM 经常胡乱聚合数据（比如把“平均气温”进行“求和”处理）。
*   **你的创新：** **“语义知识图谱 + 统计一致性检查”。**
*   **具体逻辑：** 在 LLM 生成绘图代码前，先经过一个“语义检查层”。该层利用规则或小型知识库识别列名属性（如：ID列不可求和，日期列适合做X轴）。生成后，再用代码回测结果，确保图表显示的数值与原始数据统计特征一致。
*   **研究高度：** 解决了大模型在数据科学领域的**鲁棒性与真实性（Factuality）**问题。
*   **亮点：** 你的系统会比普通的 Chat2Viz “更懂数据科学”，不容易出低级错误。

### 创新点三：基于“设计准则”的视觉自动优化（美学创新）
*   **传统做法：** AI 只管出图，不管美丑，经常出现标签重叠、配色难看的问题。
*   **你的创新：** **将可视化设计经验（Visual Design Guidelines）转化为 Prompt 约束或后处理 Agent。**
*   **具体逻辑：** 引入一个“视觉评论员（Critic Agent）”。它专门负责根据可视化理论（如 Mackinlay 的感知排序、色彩对比原则）审核生成的图表。如果发现颜色太多太杂，它会命令系统重画。
*   **研究高度：** 将**计算机图形学中的美学启发式算法**与 LLM 结合，探索“何为好的可视化”。
*   **亮点：** 你的毕设生成的图表看起来会比别人做的更专业、更像出自专业设计师之手。

### 创新点四：面向“异常挖掘”的交互式启发叙事
*   **传统做法：** 用户问什么，系统答什么。
*   **你的创新：** **“主动探测（Proactive Discovery）+ 追问式交互”。**
*   **具体逻辑：** 系统加载数据时，后台先跑异常检测（Anomaly Detection）。出图后，AI 主动高亮异常点，并给用户提供“追问建议”（例如：“我发现 3 月份销售异常，你想了解是哪些产品导致的吗？”）。
*   **研究高度：** 探索**混合主动性交互（Mixed-initiative interaction）**。
*   **亮点：** 将系统从单纯的“绘图工具”升级为“智能分析助理”，体现了对数据价值的深度挖掘。

---

### 如何在论文中突出这些创新点？

为了让评委老师认可这些创新，你需要在论文中加入以下对比：

1.  **Baseline 对比实验：** 
    *   选一个开源的 Chat2Viz 基础模型（比如直接用 GPT-4 生成代码）。
    *   展示在你的创新机制（如逻辑守卫、视觉反馈）加入后，**任务成功率**提升了多少，或者**用户纠错次数**减少了多少。
2.  **定性分析：**
    *   展示几个典型案例（Case Study）。比如：普通模型生成的图表在处理某复杂数据时崩了，而你的系统通过“自我修复”成功出图。
3.  **用户实验（核心）：**
    *   找 10 个同学，分两组：A组用普通工具，B组用你的系统。
    *   测量他们完成相同分析任务的时间。如果你的系统更快、满意度更高，这就是最有力的证据。

### 推荐毕设架构：
*   **前端：** React + ECharts。
*   **中台（你的核心创新）：** 异常检测模块 + 语义纠错模块 + 多轮对话状态管理器。
*   **后端：** Python FastAPI + LLM API（如 DeepSeek 或 GPT-4）。

**总结建议：**
如果你想拿优秀毕设，选**创新点一（交互）**或**创新点二（可靠性）**。这两个方向既有技术含量，又非常直观，论文也非常容易写出“研究味儿”。

这是一个非常好的产品思维转变！从“**数据切片（Slicing）**”进化到“**场景钻取（Drill-down）**”是专业 BI 和 AI 分析平台的分水岭。

如果仅仅是“把不相关的数据隐藏”，那是 Excel 的筛选功能。对于一个 **LLM 驱动的时空平台**，框选后的行为可以更智能、更具洞察力。

以下是几个可以基于现有架构（`VizEditor`, `Planner`, `Insight`）低成本扩展的高级功能建议：

---

### 1. 自动触发“对比分析” (Comparative Analysis)
**核心逻辑**：不仅展示框选区域的数据，还要展示它与**全局（或剩余区域）**的对比。

*   **功能描述**：
    *   当用户框选曼哈顿中城时，右侧图表自动变成“双柱图”或“虚实线对比图”。
    *   **Series A (实色)**：框选区域的数值（如：该区域平均客单价 $15）。
    *   **Series B (灰色/虚线)**：全城平均水平（如：全城平均客单价 $12）。
*   **业务价值**：用户一眼就能看出：“哦，虽然这个区域订单少，但客单价远高于全市平均水平，是高价值区域。”
*   **后端实现思路**：
    *   在 `VizEditor` 注入代码时，保留一份 `df_global`。
    *   生成的 Plotly 代码中，增加一个 Trace 绘制全局均值线。

### 2. 粒度自动下钻 (Semantic Zooming)
**核心逻辑**：空间范围变小了，时间或分类的粒度就应该变细。

*   **功能描述**：
    *   **全城视角**：右侧显示“按**天**”的趋势，或“按**行政区**”的排名。
    *   **框选视角**：
        *   时间轴自动从“天”切换为“**小时**”甚至“**15分钟**”。
        *   分类维度自动从“行政区”切换为“**街道 (Street/Zone)**”甚至“**具体 POI**”。
*   **业务价值**：宏观微观一把抓。宏观通过天看趋势，微观通过小时看早晚高峰。
*   **后端实现思路**：
    *   修改 `DashboardPlanner`。当检测到 `payload.bbox` 存在时，提示 LLM：“用户正在关注局部微观数据，请将图表的时间聚合粒度（time_bucket）设为更精细的值（如 15T），并将分组维度设为更底层的字段。”

### 3. 区域画像/标签生成 (AOI Profiling)
**核心逻辑**：利用 LLM 的知识库，告诉用户“你框选的这是个什么地方”。

*   **功能描述**：
    *   在框选后，底部的 **AI Insight** 卡片第一句话不是说数据，而是说地理语义。
    *   **例子**：“您框选的区域包含了 **JFK 机场及其周边物流园区**。数据显示该区域在深夜 2 点仍有高频订单，符合机场交通特征。”
*   **业务价值**：将冷冰冰的坐标和 ID 转化为人类可理解的**地理语义（AOI, Area of Interest）**。
*   **后端实现思路**：
    *   在 `InsightExtractor` 中，将框选留下的 `Zone Name` 列表传给 LLM。让 LLM 总结这些地点的共同特征（如：都是商业区、都是住宅区、或者是交通枢纽）。

### 4. 异常检测与归因 (Local Anomaly Detection)
**核心逻辑**：在这个区域里，有什么东西是不正常的？

*   **功能描述**：
    *   框选后，地图上不仅显示热力，还自动高亮出**“离群点”**（Outliers）。
    *   例如：在曼哈顿的一片红色热力中，突然有个街区是绿色的（订单极少），系统自动把这个街区圈出来，并提示“该街区流量显著低于周边，可能是数据缺失或道路施工”。
*   **业务价值**：帮助用户发现“盲点”或“机会点”。

---

### 如何在现有代码中快速落地？

推荐先落地 **方案 1（对比分析）** 和 **方案 2（粒度下钻）**。

#### 落地：修改 `VizEditor` 实现“对比分析”

你可以在 `VizEditor` 的注入逻辑中，计算一个 `global_mean`，并在打印日志或返回结果中带上它，供前端或 Insight 使用。

更高级的做法是，**修改 `DashboardPlanner` 的 Prompt**。

**在 `backend/core/generation/dashboard_planner.py` 中：**

```python
    async def plan_dashboard(self, query: str, summaries: List[Dict[str, Any]], payload: InteractionPayload = None) -> DashboardSchema:
        # ...
        
        # 增加一个逻辑判断
        is_drill_down = payload and payload.bbox is not None
        
        system_prompt = f"""
        ...
        === 动态规划规则 ===
        { "当前用户正在进行【区域框选】，请务必规划更微观的图表，例如将时间粒度细化到'小时'，并尝试对比该区域与全局的均值。" if is_drill_down else "当前为全览模式，请规划宏观统计图表。" }
        ...
        """
```

通过这种方式，当你框选时，Planner 会感知到上下文的变化，从而指示 Generator 生成完全不同维度的图表（比如从“各区排名”变成“该区域内各街道排名”），这就不仅仅是简单的过滤了，而是真正的**分析视角的切换**。

